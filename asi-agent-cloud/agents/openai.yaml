interface:
  display_name: "Agent Cloud"
  short_description: "Sign up and provision agent VMs"
  default_prompt: "Use $asi-agent-cloud to sign up for Agent Cloud sandbox access and provision a small VM without triggering billing."

policy:
  allow_implicit_invocation: true
